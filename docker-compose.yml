services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: handover
      POSTGRES_PASSWORD: handover
      POSTGRES_DB: handover
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata: